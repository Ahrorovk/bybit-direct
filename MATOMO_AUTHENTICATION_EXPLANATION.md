# Как Matomo находит сервер и работает без логина/пароля

## 🔐 Аутентификация в Matomo

### Трекер (отправка данных) - НЕ требует аутентификации

**Трекер Matomo** (`index.php` или `matomo.php`) - это **публичный endpoint**, который:
- ✅ **НЕ требует** логина и пароля
- ✅ Принимает данные от любого клиента
- ✅ Идентифицирует сайт по **Site ID**

### Админ-панель - требует аутентификацию

**Админ-панель Matomo** (`/index.php` для входа) - требует:
- ❌ Логин и пароль
- ❌ Используется только для просмотра отчетов и настройки

## 📍 Как приложение находит сервер

### 1. URL сервера указывается в `strings.xml`

```xml
<string name="matomo_url">https://matomo.private-analytic-tools.com</string>
```

### 2. Приложение использует этот URL для:
- Загрузки контейнера: `https://matomo.private-analytic-tools.com/js/container_HNSAYDRE.js`
- Отправки данных: `https://matomo.private-analytic-tools.com/index.php`

### 3. Site ID идентифицирует сайт

```xml
<string name="matomo_site_id">2</string>
```

Site ID говорит Matomo, для какого сайта сохранять данные.

## 🔄 Как работает процесс отправки данных

1. **Приложение загружается**
   - Читает `matomo_url` из `strings.xml`
   - Читает `matomo_site_id` из `strings.xml`

2. **Инициализация Matomo**
   ```javascript
   _paq.push(['setTrackerUrl', 'https://matomo.private-analytic-tools.com/index.php']);
   _paq.push(['setSiteId', '2']);
   ```

3. **Отправка данных**
   - Приложение отправляет HTTP запрос на `https://matomo.private-analytic-tools.com/index.php`
   - В запросе передается:
     - `idsite=2` (Site ID)
     - Данные о посещении (URL, заголовок, время и т.д.)
     - Custom Dimensions (разрешение экрана, модель устройства и т.д.)

4. **Сервер Matomo**
   - Принимает запрос (без проверки логина/пароля)
   - Проверяет Site ID (должен существовать)
   - Сохраняет данные в базу данных

## 🔒 Безопасность

### Почему это безопасно:

1. **Site ID - это не пароль**
   - Site ID можно узнать из кода сайта (он публичный)
   - Но без доступа к админ-панели нельзя:
     - Изменить настройки
     - Удалить данные
     - Просмотреть детальные отчеты

2. **Защита от спама:**
   - Можно настроить фильтры в Matomo
   - Можно ограничить по IP
   - Можно использовать токен аутентификации (опционально)

3. **Токен аутентификации (опционально):**
   Если хотите дополнительную защиту, можно использовать токен:
   ```javascript
   _paq.push(['setApiUrl', 'https://matomo.private-analytic-tools.com/index.php']);
   _paq.push(['setTokenAuth', 'YOUR_TOKEN']);
   ```
   Но для обычного трекинга это не обязательно.

## 📊 Проверка работы

### 1. Проверка URL в коде:
```kotlin
Log.d(TAG, "Tracker URL: $matomoUrl/index.php")
Log.d(TAG, "Site ID: $siteId")
```

### 2. Проверка запросов:
В логах Android Studio вы увидите:
```
🔵 Matomo Request intercepted: https://matomo.private-analytic-tools.com/index.php?idsite=2&...
```

### 3. Проверка в Matomo:
- Откройте Matomo админ-панель (с логином/паролем)
- Перейдите в "В реальном времени"
- Должны появиться посещения

## ✅ Итого

- ❌ **Логин/пароль НЕ нужны** для отправки данных
- ✅ **URL сервера** указывается в `strings.xml`
- ✅ **Site ID** идентифицирует сайт
- ✅ **Трекер** - публичный endpoint
- 🔒 **Админ-панель** требует аутентификацию (но это отдельно)

Приложение просто отправляет HTTP запросы на указанный URL с Site ID, и Matomo сохраняет данные.





